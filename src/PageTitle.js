export const PageTitle = ({ text }) => {
  return (
    <section className="section">
      <div className="block">
        <h1 className="is-size-2 has-text-centered has-text-light">{text}</h1>
      </div>
    </section>
  );
};
